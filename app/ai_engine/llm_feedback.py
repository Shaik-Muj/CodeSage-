"""Generate feedback using an LLM (placeholder).
Replace with real model calls when configured.
"""


def critique_code_placeholder(code: str) -> str:
    return "Looks good — small placeholder critique."
